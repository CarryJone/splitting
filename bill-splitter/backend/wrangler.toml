name = "bill-splitter-backend"
main = "src/index.ts"
compatibility_date = "2024-12-05"
compatibility_flags = ["nodejs_compat"]

[vars]
# DATABASE_URL will be set via secrets in production, but for dev we use .env
# We can also set a default or dummy here if needed, but usually we rely on .dev.vars or .env
